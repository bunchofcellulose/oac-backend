# Environment variables for Railway
NODE_ENV=production
PORT=3000

# Email configuration (to be set in Railway dashboard)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Optional: Database URL (if you plan to use a database later)
# DATABASE_URL=

# Optional: Redis URL (if you plan to use Redis for caching)
# REDIS_URL=
